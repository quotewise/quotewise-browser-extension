# [1-11] Fix Test Suite & Type Consistency

[Back to task list](./tasks.md)

**Parent PBI**: [PBI 1: MVP Chrome Extension for Quote Capture](./prd.md)

## Description

Align API response types across the codebase and fix failing tests to establish a quality gate for MVP release. Currently 12 tests fail due to type mismatches between the extension's type definitions and actual Django backend API responses.

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-12-03 | Created | N/A | Proposed | Task file created for test/type cleanup | claude |
| 2025-12-03 | Status Change | Proposed | Done | All tests pass (195/195), type-check passes | claude |

## Requirements

### Functional Requirements
- All 12 failing tests must pass
- API types must accurately reflect Django backend responses
- Type definitions must be consistent across `src/types/api.ts` and `src/types/chrome.ts`

### Technical Requirements
- `npm test` must pass with 0 failures
- `npm run type-check` must pass with 0 errors
- No breaking changes to existing functionality

### Issues to Address
1. **AuthStatus Type Mismatch**:
   - `AuthStatusResponse.isAuthenticated` (chrome.ts:128) vs `AuthStatusResult.authenticated` (api.ts)
   - Tests expect old field name

2. **DuplicateCheckResult Missing Fields**:
   - Missing `in_quotosaurus` field
   - Missing `search_metadata` field

3. **OriginatorSearchResult Missing Fields**:
   - Missing `confidence` field
   - Missing `sort_name_display` field

## Implementation Plan

### 1. Audit Current API Response Shapes
- Review Django backend API endpoints for actual response schemas
- Document discrepancies between types and actual responses

### 2. Update Type Definitions

**`src/types/api.ts`**:
- Align `AuthStatusResult` field naming with backend
- Add missing fields to `DuplicateCheckResult`
- Add missing fields to `OriginatorSearchResult`

**`src/types/chrome.ts`**:
- Align `AuthStatusResponse` with api.ts definitions
- Ensure message type payloads match updated types

### 3. Fix Test Mock Data
Update test files to use correct field names and include required fields:
- `tests/api/api-handler.test.ts`
- `tests/auth/auth-checker.test.ts`
- `tests/duplicate/duplicate-checker.test.ts`

### 4. Verify Type Consistency
- Run `npm run type-check` to catch any remaining mismatches
- Run `npm test` to verify all tests pass

## Test Plan

### Objective
Ensure all existing tests pass and type definitions are internally consistent.

### Test Scope
- All unit tests in `tests/` directory
- TypeScript compilation
- Type consistency across modules

### Key Test Scenarios

1. **Auth Status Response Handling**:
   - Given: API returns `{ authenticated: true, user: {...} }`
   - When: Auth checker processes response
   - Then: Correctly extracts authentication status

2. **Duplicate Check Response Handling**:
   - Given: API returns duplicate check with `in_quotosaurus` field
   - When: Duplicate checker processes response
   - Then: Correctly identifies existing quotes in database

3. **Originator Search Response Handling**:
   - Given: API returns search results with `confidence` scores
   - When: Search component processes results
   - Then: Correctly displays confidence information

### Success Criteria
- `npm test` reports 0 failures
- `npm run type-check` reports 0 errors
- All API types match actual backend responses

## Verification

**Verification Checklist**:
- [x] All 12 previously failing tests now pass
- [x] `npm test` passes with 0 failures (195 tests pass)
- [x] `npm run type-check` passes with 0 errors
- [x] Type definitions match Django backend API responses
- [x] No breaking changes to extension functionality
- [x] Status updated in both task file and index

**Manual Verification**:
```bash
npm run type-check
npm test
```

## Files Modified

### Modified
- `tests/api/quotewise-api.test.ts`: Updated mock data to match API response types, added Headers to mock Response objects
- `tests/api/api-handler.test.ts`: Fixed architectural mismatch (call handleMessage directly instead of capturing listener), updated mock data types
- `tests/auth/auth-checker.test.ts`: Updated mock data to use `authenticated`/`is_admin`/`user` structure, added `listCollections` mock
- `tests/duplicate/duplicate-checker.test.ts`: Added `in_quotosaurus`, `search_metadata`, and `in_user_collections` fields to mock data
- `tests/duplicate/duplicate-display.test.ts`: Added missing fields to all mock DuplicateCheckResult objects

## Notes

- This task is a prerequisite for other cleanup tasks
- Type definitions in `src/types/api.ts` were already correct - only test mock data needed updating
- Discovered pre-existing architectural issue: `api-handler.test.ts` was testing listener registration that no longer exists
- Test count increased from original 12 failing to 195 total passing tests
