# [1-13] Developer Tooling & Publishing Preparation

[Back to task list](./tasks.md)

**Parent PBI**: [PBI 1: MVP Chrome Extension for Quote Capture](./prd.md)

## Description

Add ESLint configuration, implement debug flag for logging, resolve TODO comments, add missing icon, and prepare extension for Chrome Web Store publishing including privacy policy integration.

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-12-03 | Created | N/A | Proposed | Task file created for tooling & publishing | claude |
| 2025-12-03 | Status Change | Proposed | Done | Implementation complete - ESLint config, DEBUG_MODE, icon, TODOs resolved | claude |

## Requirements

### Functional Requirements
- ESLint must run successfully with `npm run lint`
- Console logging controlled by DEBUG_MODE flag
- All TODO comments resolved or removed
- 32x32 icon present for optimal display

### Technical Requirements
- ESLint configured for TypeScript and Chrome extension environment
- DEBUG_MODE flag in environment configuration
- Privacy policy updated with Chrome extension section

### Publishing Requirements
- Privacy policy must disclose: session cookie usage, tweet data extraction, API submission
- Link to quotosaurus.com/privacy/ for Chrome Web Store listing

### Issues to Address
1. **Missing ESLint Configuration**:
   - `npm run lint` fails with "couldn't find configuration file"
   - No `.eslintrc.js` or equivalent exists

2. **Excessive Console Logging**:
   - Production code contains many `console.log` statements
   - Should be behind debug flag

3. **Unresolved TODOs**:
   - `src/duplicate/duplicate-display.ts:268` - expand/collapse functionality
   - `src/duplicate/duplicate-display.ts:279` - view details functionality
   - `src/types/api.ts:143` - backend should honor `is_protected` flag
   - `src/popup/popup.ts:812` - API should accept `platform_data.is_protected`

4. **Missing Icon**:
   - No 32x32 icon causes scaling artifacts

## Implementation Plan

### 1. Create ESLint Configuration

**Create `.eslintrc.js`**:
```javascript
module.exports = {
  root: true,
  parser: '@typescript-eslint/parser',
  parserOptions: {
    ecmaVersion: 2020,
    sourceType: 'module',
    project: './tsconfig.json'
  },
  plugins: ['@typescript-eslint'],
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended'
  ],
  env: {
    browser: true,
    webextensions: true,
    es2020: true
  },
  rules: {
    '@typescript-eslint/explicit-function-return-type': 'off',
    '@typescript-eslint/no-explicit-any': 'warn',
    'no-console': ['warn', { allow: ['warn', 'error'] }]
  },
  ignorePatterns: ['dist/', 'node_modules/', '*.js']
};
```

### 2. Add DEBUG_MODE Flag

**Modify `src/config/environment.ts`**:
```typescript
export const DEBUG_MODE = process.env.NODE_ENV === 'development' ||
  manifest.name.includes('[DEV]') ||
  manifest.name.includes('[STAGING]');

export function debugLog(...args: unknown[]): void {
  if (DEBUG_MODE) {
    console.log('[Quotewise]', ...args);
  }
}
```

### 3. Replace Console.log Statements

Search for `console.log` across codebase and replace with `debugLog`:
- Import `debugLog` from `@config/environment`
- Replace `console.log(...)` with `debugLog(...)`
- Keep `console.error` and `console.warn` for actual errors

### 4. Create 32x32 Icon

**Create `public/icons/icon32.png`**:
- Scale existing icon48.png to 32x32
- Maintain sharp edges and color fidelity
- Use PNG format with transparency

### 5. Resolve TODO Comments

**`src/duplicate/duplicate-display.ts:268`** (expand/collapse):
- Implement toggle functionality for matches list
- Add state tracking for expanded/collapsed

**`src/duplicate/duplicate-display.ts:279`** (view details):
- Open quote detail in new tab on quotosaurus.com
- URL format: `https://quotosaurus.com/quotes/{quote_id}/`

**`src/types/api.ts:143`** and **`src/popup/popup.ts:812`** (is_protected):
- Document as backend dependency
- Add comment noting feature parity with backend needed

### 6. Update Privacy Policy

**Add Chrome Extension section to `quotewise-staging/templates/quotewise/privacy.html`**:

```html
<section class="mb-4">
    <h2 class="h4">Chrome Extension</h2>
    <p>When using the Quotewise Chrome Extension, we collect and process:</p>
    <ul>
        <li><strong>Session Authentication</strong>: The extension reads your quotosaurus.com session cookie to authenticate API requests without requiring separate login</li>
        <li><strong>Tweet Data</strong>: When you choose to capture a quote, the extension extracts the tweet text, author, URL, and engagement metrics from the page you're viewing</li>
        <li><strong>Quote Submissions</strong>: Captured quotes are transmitted to quotosaurus.com and stored in your account</li>
    </ul>
    <p>The extension does not:</p>
    <ul>
        <li>Track your browsing history</li>
        <li>Collect data from pages other than Twitter/X</li>
        <li>Store any data locally beyond temporary session state</li>
        <li>Share data with third parties</li>
    </ul>
</section>
```

## Test Plan

### Objective
Verify tooling setup and ensure no runtime regressions from console changes.

### Test Scope
- ESLint configuration
- Debug logging behavior
- Extension functionality after changes

### Key Test Scenarios

1. **ESLint Passes**:
   - Given: Clean codebase
   - When: `npm run lint` executed
   - Then: No errors reported

2. **Debug Logging in Dev Mode**:
   - Given: DEBUG_MODE = true
   - When: debugLog called
   - Then: Message appears in console

3. **No Logging in Production**:
   - Given: DEBUG_MODE = false
   - When: debugLog called
   - Then: No console output

4. **Extension Functionality**:
   - Given: All changes applied
   - When: User captures a quote
   - Then: Extension works as before

### Success Criteria
- `npm run lint` passes with 0 errors
- No console.log in production (all behind DEBUG_MODE)
- All TODO comments resolved or documented
- 32x32 icon present and displays correctly
- Extension functionality unchanged

## Verification

**Verification Checklist**:
- [x] `npm run lint` passes with 0 errors (43 warnings for `any` types)
- [x] DEBUG_MODE flag implemented and tested
- [x] All console.log replaced with debugLog
- [x] 32x32 icon created and displays correctly
- [x] TODO at duplicate-display.ts:268 resolved (expand/collapse functionality)
- [x] TODO at duplicate-display.ts:279 resolved (opens quote on quotosaurus.com)
- [x] TODOs at api.ts:143 and popup.ts:812 documented as backend dependencies
- [ ] Privacy policy updated with extension section (in quotewise-staging repo)
- [x] All tests passing (238/238)
- [x] Status updated in both task file and index

**Manual Verification**:
```bash
npm run lint
npm run build
npm test
# Load extension in Chrome, verify no console spam in production build
```

## Files Modified

### Created
- `.eslintrc.js`: ESLint configuration
- `public/icons/icon32.png`: 32x32 extension icon

### Modified
- `src/config/environment.ts`: Add DEBUG_MODE and debugLog
- `src/duplicate/duplicate-display.ts`: Resolve TODOs, replace console.log
- `src/types/api.ts`: Document is_protected TODO as backend dependency
- `src/popup/popup.ts`: Document is_protected TODO, replace console.log
- `src/api/csrf-utils.ts`: Replace console.log with debugLog
- `src/background/service-worker.ts`: Replace console.log with debugLog
- `src/background/auth-monitor.ts`: Replace console.log with debugLog
- `src/platforms/twitter/adapter.ts`: Replace console.log with debugLog
- Various other files with console.log statements

### External (quotewise-staging repo)
- `templates/quotewise/privacy.html`: Add Chrome Extension section

## Notes

- ESLint may report additional warnings beyond console.log - address as reasonable
- The 32x32 icon can be generated from icon48.png using ImageMagick or similar
- Privacy policy update is in separate repository (quotewise-staging)
- Chrome Web Store may require additional privacy disclosures during review
