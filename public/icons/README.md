# Extension Icons

Branded Quotewise toolbar/store icons at 16, 32, 48, and 128 px, each with a
`-grey` inactive-state variant (e.g. `icon16.png` / `icon16-grey.png`). Regenerate
with `bun run icons` (see `scripts/generate-icons.mjs`).
