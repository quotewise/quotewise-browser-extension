# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# This project uses Bun (bun.lock) — keep other package managers' lockfiles out.
# A stale package-lock.json diverges from the bun tree and gets scanned by
# Dependabot in isolation (see CLAUDE.md "Always use Bun").
package-lock.json
yarn.lock
pnpm-lock.yaml

# Build outputs
dist/
dist-firefox/
build/
web-ext-artifacts/

# Environment variables
.env*
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# AI-agent tooling trees — kept on disk for local agent workflows, not published
.agents/
.claude/
.codex/
.specify/
.mcp.json
.playwright-mcp/

# Internal pre-ship security review (resolved punch-list; not for public consumption)
README-pre-ship-review.md

# Audit fixtures: real third-party DOM captures used for internal platform-adapter work
specs/*/audits/

# Historical MVP-phase delivery/PBI docs — internal planning, superseded architecture
docs/delivery/

# Maintainer's private working notes — never published
docs/untracked/

# Internal issue-tracker store (local tooling; machine-specific, not published)
.beads/

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Test coverage
coverage/
*.lcov

# Logs
logs
*.log

# Chrome extension development
*.crx
*.pem

# Packaged extension (Chrome Web Store upload artifact)
quotewise-extension-*.zip

# Temporary files
*.tmp
tmp/
temp/
.superpowers/
